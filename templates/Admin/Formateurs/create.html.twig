{% extends "base.html.twig" %}

{% block title %}Création d'un compte
{% endblock %}

{% block body %}
	<h1>Créer un compte utilisateur</h1>
	<a href="{{path('admin.index')}}">Retour à la liste</a>

	{# {{form_start(form, {'action': path('app_infos_user'), 'method': 'POST'})}}
		{{form_row(form)}}
		{{form_row(form)}}
		{{form_end(form)}} #}
	{{form_start(form)}}
	{{form_widget(form)}}
	<button>Créer le compte</button>
	{{form_end(form)}}
{% endblock %}
